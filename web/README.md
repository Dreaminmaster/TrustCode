# TrustCode Web Version

This folder is the structured development version of the local HTML app.

The root `index.html` remains a standalone single-file version that can be opened directly in a browser.

Recommended development structure:

```text
web/
├── index.html
├── styles.css
└── app.js
```

Use this folder when you want to maintain or extend the web app more cleanly.
