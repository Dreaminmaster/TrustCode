const issuer='TrustCode';const account='shared-user';let currentSecret='';function toBase32(bytes){const a='ABCDEFGHIJKLMNOPQRSTUVWXYZ234567';let bits='',out='';bytes.forEach(b=>bits+=b.toString(2).padStart(8,'0'));for(let i=0;i<bits.length;i+=5){out+=a[parseInt(bits.slice(i,i+5).padEnd(5,'0'),2)]}return out}function makeSecret(){const bytes=new Uint8Array(20);crypto.getRandomValues(bytes);return toBase32(bytes)}function makeUri(secret){const label=encodeURIComponent(`${issuer}:${account}`);const q=new URLSearchParams({secret,issuer,algorithm:'SHA1',digits:'6',period:'30'});return `otpauth://totp/${label}?${q.toString()}`}function render(){currentSecret=makeSecret();document.getElementById('secret').textContent=currentSecret;QRCode.toCanvas(document.getElementById('qrCanvas'),makeUri(currentSecret),{width:190,margin:1})}document.getElementById('newBtn').addEventListener('click',render);document.getElementById('copyBtn').addEventListener('click',async()=>{await navigator.clipboard.writeText(currentSecret);const btn=document.getElementById('copyBtn');btn.textContent='Copied';setTimeout(()=>btn.textContent='Copy',1000)});render();